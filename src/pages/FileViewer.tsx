import { useSearchParams } from 'react-router-dom'

const FileViewer = () => {
  useSearchParams();
    // console.log(searchParams)  
  return (
    <div>FileViewer</div>
  )
}

export default FileViewer